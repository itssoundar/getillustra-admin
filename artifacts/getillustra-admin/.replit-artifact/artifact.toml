kind = "web"
previewPath = "/"
title = "GetIllustra Admin"
version = "1.0.0"
id = "artifacts/getillustra-admin"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24250

[services.development]
run = "pnpm --filter @workspace/getillustra-admin run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/getillustra-admin", "run", "build" ]
publicDir = "artifacts/getillustra-admin/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24250"
BASE_PATH = "/"
